import 'firebase_core_service.dart';

class FirebaseCorePlatform extends FirebaseCoreService {
  @override
  Future<void> initializeApp() async {
    // Firebase initialization is handled by the platform-specific implementations
    // This is a stub implementation for unsupported platforms
  }
}
